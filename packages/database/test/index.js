'use strict'

const { test, skip } = require('tap')

test('Database', async () => {
  skip('creates connection', async t => {
    t.fail()
  })

  skip('fails to create connection for non-available client', async t => {
    t.fail()
  })
})
