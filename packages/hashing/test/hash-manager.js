'use strict'

const { expect } = require('expect')
const { test } = require('@japa/runner')
// const { HashManager } = require('../dist')

test.group('HashManager', () => {
  test('hash a value', async () => {
    expect(true).toBe(false)
  }).skip()
})
